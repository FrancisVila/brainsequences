Build a route named /brainparts leading to a component Bainparts.jsx. The page shows a table where each row is one brainpart. The table has one column for each attribute of the brainparts table. Each row has a delete icon and an edit icon, in separate columns. Create routes and components for /brainparts/create, /brainparts/update, /brainparts/read. Above the table, a large + icon to create a new brainpart. Didplay a modal requesting confirmation when the user clicks on a delete icon.