in Brain3dviewer, on each canvas OrthogonalSliceView, add 2 segments to represent the position of the slice on the other 2 axes. When the user moves the cursor on axis x, the 2 segments on axis x should move to follow the position. The color of each segment should correpond to the axis color. For example, on the X axis OrthogonalSliceView, the Y segment should be green and the Z axis should be blue
