visit each page listed in the file wikimedia2.txt
look for images referenced in each page, that satisfy the regex search /<img[^>]*src="([^>]*\.gif)/
the url of the image should be the part between parenthesis ()
download each image into the folder wikimedia2/
If you can find no file corresponding to a pattern, report this in an output file