In the turso database, add a folder column (boolean, default value false) to the brainparts table, to indicate that the brainpart is only a folder and not a part to be represented in the svg diagram. In the #brainpartTree (inside BrainpartTree.tsx), give the folder brainparts a specific className. Also do not show the subparts of a folder inside the #atlas-image-container (in AtlasImage.tsx). Set the brainpart titled 'Brodmann Areas' to folder=true.