Create a modal component to help the user in edit mode to select which svg image to use in a step or a sequence.
the modal should show, for each svg image in the app/images/atlasSvg folder: 
* the title of the image, transformed from the name of the file, with '_' replaced with ' ' and first letter uppercase.
* a reduced-size version of the svg 
* if available, the text of the .md file in the same folder and with the same name as the .svg file. The file should be formatted according to .md rules