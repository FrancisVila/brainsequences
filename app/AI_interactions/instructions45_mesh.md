1) I can see only part of the cuneus region of the brain. I want to see the whole brain, with the cuneus in color
2) Now when I move the mouse on the X axis, that moves the mesh along the X axis. I want it to move not the mesh on the X axist, but the cross on X axis. This should then move the mesh on the other two axes (Z and Y). If the user moves the cursor up / down on X, that should move the Z axis up or down, and the if she moves the cursor left/right, it should move the Y axis left right. Same for the other two axes: up/down on Y = move Z, left/right on Y=move X, left/right on Z=move X, up/down on Z=move Y.
3) the brain inside each X, Y, Z is out of center, half of it is hidden. Also the part of the brain outside the Cuneus region is hardly visible.
4) the movement of the cursor should not be measured by the absolute position of the cursor on the pane, but by the relative motion of the cursor. For example, if the current position is 30 and the user clicks on 50 and moves up to 60, that should make the current position up to 40.
